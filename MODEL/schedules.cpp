#include "schedules.h"


Schedules::Schedules() : L() {}

/*Schedules::get_corse() const {
    Corsa* p = nullptr;
    Dlista<Workout*> restituire;

    for(vector<Workout*>::const_iterator cit = l.begin(); cit!=brani.end(); ++cit) {
     p=dynamic_cast<Corsa*>((*cit)->ptr);
     if(((*cit)->ptr)->qualita() && (!p || p->getLossLess()))
     v.push_back((*cit)->ptr);
    }

}*/


/*
std::vector<FileAudio*> braniQual() const {
   WAV* p =nullptr;
   std::vector<FileAudio*> v;
   for(vector<Brano*>::const_iterator cit = brani.begin(); cit!=brani.end(); ++cit) {
     p=dynamic_cast<WAV*>((*cit)->ptr);
     if(((*cit)->ptr)->qualita() && (!p || p->getLossLess()))
 v.push_back((*cit)->ptr);
   }
   return v;
 }
*/
